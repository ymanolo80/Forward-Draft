# Forward Draft

Forward Draft is a React and Vite writing workspace for drafting, reviewing, and rewriting script projects.

## Local Development

```sh
npm install
npm run web
```

The local app runs at `http://127.0.0.1:5173/`.

## Build

```sh
npm run build
```
